container_commands:
    00composer_clear:
        command: "php artisan config:clear"
    01generate_tables:
        command: "php artisan migrate --force"
    02generate_data:
        command: "php artisan db:seed --force"
